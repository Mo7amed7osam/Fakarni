# Fakarni Landing

This folder is intentionally separate from the Expo app target.

- `index.html` is a standalone marketing page.
- `styles.css` contains all landing styles.
- Screenshot blocks are placeholders and should be replaced with final App Store or product images.

This keeps the iOS app project focused on App Store packaging and review flow, without embedding the landing page into `App.tsx`.
